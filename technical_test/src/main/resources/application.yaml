spring:
  application:
    name: technical_test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: test1234
    url: jdbc:mysql://localhost:3306/technical_test?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
  jpa:
      hibernate:
        ddl-auto: update
      open-in-view: false
      properties:
        hibernate:
          format_sql: true
          show_sql: true
logging:
    level:
      root: INFO

