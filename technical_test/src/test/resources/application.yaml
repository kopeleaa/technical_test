spring:
  application:
    name: technical_test
  datasource:
    driver-class-name: org.h2.Driver
    password:
    url: jdbc:h2:mem:technical_test
    username: sa

  h2:
    console:
      enabled: true
      path: /database
  jpa:
    open-in-view: false